import styled from "styled-components";

export const Wrapper = styled.div`
  padding: 0 20px;
  text-align: center;

  h1 {
    margin: 0;
    padding: 10px;
    color: var(--darkGrey);
    font-size: var(--fontHuge);
  }
`;
